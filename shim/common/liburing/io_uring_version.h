/* Also a file liburing's configure generates. The numbers are the
 * liburing this repo carries the seam for - update them when that
 * liburing moves. */
#ifndef LIBURING_VERSION_H
#define LIBURING_VERSION_H

#define IO_URING_VERSION_MAJOR 2
#define IO_URING_VERSION_MINOR 13

#endif
